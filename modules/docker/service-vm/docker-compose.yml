services:

  # ── Shared Infrastructure ─────────────────────────────
  # Redis: shared between Immich and Paperless
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    networks:
      - backend

  # ── Immich ────────────────────────────────────────────
  # Requires pgvecto-rs PostgreSQL extension
  immich-postgres:
    image: tensorchord/pgvecto-rs:pg16-v0.2.0
    container_name: immich-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: immich
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_INITDB_ARGS: "--data-checksums"
    volumes:
      - immich_db:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: pg_isready --dbname='immich' --username='${DB_USER}' || exit 1
      interval: 5s
      timeout: 5s
      retries: 10

  immich-server:
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    container_name: immich-server
    restart: unless-stopped
    ports:
      - "2283:2283"
    depends_on:
      immich-postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - /mnt/nas/media/photos:/usr/src/app/upload
    environment:
      DB_HOSTNAME: immich-postgres
      DB_DATABASE_NAME: immich
      DB_USERNAME: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      REDIS_HOSTNAME: redis
    env_file: .env
    networks:
      - backend

  immich-machine-learning:
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}
    container_name: immich-machine-learning
    restart: unless-stopped
    volumes:
      - immich_ml_cache:/cache
    networks:
      - backend

  # ── Paperless-ngx ─────────────────────────────────────
  paperless-postgres:
    image: postgres:16-alpine
    container_name: paperless-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - paperless_db:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: pg_isready --dbname=paperless --username='${DB_USER}' || exit 1
      interval: 5s
      timeout: 5s
      retries: 10

  paperless:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: paperless
    restart: unless-stopped
    ports:
      - "8000:8000"
    depends_on:
      paperless-postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - /mnt/nas/documents/paperless:/usr/src/paperless/media
      - /mnt/nas/documents/paperless-consume:/usr/src/paperless/consume
      - paperless_data:/usr/src/paperless/data
    environment:
      PAPERLESS_REDIS: redis://redis:6379
      PAPERLESS_DBHOST: paperless-postgres
      PAPERLESS_DBNAME: paperless
      PAPERLESS_DBUSER: ${DB_USER}
      PAPERLESS_DBPASS: ${DB_PASSWORD}
      PAPERLESS_SECRET_KEY: ${PAPERLESS_SECRET_KEY}
      PAPERLESS_ADMIN_USER: ${PAPERLESS_ADMIN_USER}
      PAPERLESS_ADMIN_PASSWORD: ${PAPERLESS_ADMIN_PASSWORD}
      PAPERLESS_TIME_ZONE: ${TZ}
      PAPERLESS_OCR_LANGUAGE: deu+eng
    networks:
      - backend

networks:
  backend:
    driver: bridge

volumes:
  redis_data:
  # DB and ML cache stay on local VM disk (NFS too slow for databases)
  immich_db:
  immich_ml_cache:
  paperless_db:
  paperless_data:
